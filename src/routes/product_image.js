// import express from "express";
// import { create, update, get, getAll, remove } from "../controllers/product_image";
// import authenticate from "../middlewares/authenticate";

// const router = express.Router();

// router.get("/product-images", getAll);
// router.get("/product-images/:id", get);
// router.post("/product-images", authenticate, create);
// router.patch("/product-images/:id", authenticate, update);
// router.delete("/product-images/:id", authenticate, remove);

// export default router;